# ─────────────────────────────────────────────
#  Resume Tailoring & Cover Letter Generator
#  requirements.txt
# ─────────────────────────────────────────────

# Streamlit – UI framework (free, OSS)
streamlit>=1.35.0

# python-docx – .docx read/write (free, OSS)
python-docx>=1.1.2

# SAP Generative AI Hub SDK – AI Core calls
generative-ai-hub-sdk>=3.2.0

# Force pydantic / pydantic-core versions that ship pre-built wheels for
# Python 3.14.  The SDK pins pydantic==2.10.6 which drags in pydantic-core
# 2.27.2, but that version has no Python 3.14 wheel and requires a Rust
# compile that fails on Streamlit Community Cloud.  Overriding here with
# versions that have official 3.14 wheels avoids the compile entirely.
pydantic>=2.11.0
pydantic-core>=2.33.0
# python-dotenv – load credentials from .env file (free, OSS)
python-dotenv>=1.0.1

# lxml – faster XML backend for python-docx (free, OSS)
lxml>=5.2.2

# Pillow – image handling in Streamlit (free, OSS, often a transitive dep)
Pillow>=10.3.0